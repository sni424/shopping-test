'use client';

import styled from 'styled-components';

export const HomeItemDiv = styled.div`
    margin-top: 2rem;
    width: '100%';
    height: '100%';
    display: flex;
    align-items: center;
    justify-content: start;
    flex-wrap: wrap;
`;
