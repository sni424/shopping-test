import styled from 'styled-components';

export const PageName = styled.span`
    font-size: 2rem;
    font-weight: 800;
    color: #2d2b3f;
`;

export const HeartItem = styled.div`
    margin-top: 2rem;
    width: '100%';
    height: '100%';
    display: flex;
    align-items: center;
    justify-content: start;
    flex-wrap: wrap;
`;
